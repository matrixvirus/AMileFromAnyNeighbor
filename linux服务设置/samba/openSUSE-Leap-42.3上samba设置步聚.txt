1
	2018年02月06日
	本机openSUSE-Leap-42.3设置samba,192.168.100.174，
	虚拟机CentOS6.9,192.168.100.173作客户端。

	本来应该用windows虚拟机做客户端实验，我虚拟机装windows系统时遇到些不明白的失
	败。

2
2.1 名词介绍
	1 SMB
	全称是Server Message Block，基于NetBIOS服务。微软发明这个协议所以其
	它系统可能连接windows机器。
	2 CIFS
	全称是Common Internet File System,是另一种samba支持的协议，定义了
	一种标准远程文件系统访问协议。
	3 NetBIOS
	是一种API,为通信的机器提供一种名字服务，允许连接的机器预约一个
	名字，机器可以被用这个名字定位。每天机器可以有任意多个名字，只要不重复。
	
	NetBIOS定义的名字和/etc/hosts还有DNS的没有关系。
	4 DC
	Domain Controller,处理一个域中的帐户的服务器。

	我只是照翻英文，摸不着头脑。
2.2 samba的三个进程	
	1 smbd
	  提供共享服务，占用两个端口139,445。	
	2 nmbd
	  提供名字服务。
	3 winbindd
3 
	安装
	$ zypper install samba
	启动
	$ systemctl start smb.service
4 配置
	主配置文件是/etc/samba/smb.conf
	这个文件有七大部分
		global homes profiles users groups printers print$
4.1 
	用testparm检查/etc/samba/smb.conf有没有错误
	$ testparm
4.2 为samba添加用户
	$ smbpasswd -a
	上条命令为samba添加了用户root
	$ smbpasswd -a USER_NAME
	为samba添加用户USER_NAME,所添用户必须在操作系统上存在。
		
	。。。
	暂存	
